# 去 AI 味改写 prompt · v8

继承 v5 框架 + v6/v7 增量，本轮加观点增量：

**C-立场·第二支点**：除了文章已有的那个排他判断，再补一个**维度不同、甚至方向对立**的判断（比如"做最强模型"押一家、"最先赚到钱"押另一家），并点明这是两个不同的赛道。两个有代价、可被反驳的判断，比一个更像真人——真人很少只有一个观点。注意：第二个判断必须同样建立在数据上，不是为了对立而对立。
