# 去 AI 味改写 prompt · v6

基于 v5 的两步框架（完整规则见 [`v5.md`](v5.md)），本轮只加一条语言增量：

**C-语言·清洗**：改完后单独过一遍"动词与虚字"。把"做了 / 进行了 / 对了一遍"这类软动词换成更利落的实义动词（核、扒、压、切、接）；删掉"不过话说回来""一种""的一个"这类可有可无的虚字。目标是让腔调更紧、更像一个说话干脆的人，而不是更口语。
